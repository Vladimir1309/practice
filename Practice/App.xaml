<Application x:Class="Practice.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Practice"
             StartupUri="MainWindow.xaml" >
    <Application.Resources>

        <Style TargetType="Window">
            <Setter Property="Width" Value="1440"/>
            <Setter Property="Height" Value="1024"/>
        </Style>
        
        <Style TargetType="TextBox">
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="13" />
                </Style>
            </Style.Resources>
        </Style>

        <Style TargetType="Button">
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="13" />
                </Style>
            </Style.Resources>
        </Style>

        <Style TargetType="Button" x:Key="blockbutt">
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="0" />
                </Style>
            </Style.Resources>
        </Style>

        <Style TargetType="Border">
            <Setter Property="CornerRadius" Value="13" />
        </Style>
        <!--<Style TargetType="Border" x:Key="BR1">
            <Setter Property="CornerRadius" Value="13" />
        </Style>-->

        <!--<Style TargetType="TextBlock" x:Key="E_mail_button">
            <Setter Property="Opacity" Value="0.13"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="True">
                    <Setter Property="Opacity" Value="1"/>
                    <Setter Property="Foreground" Value="#B70000"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.13"/>
                    <Setter Property="Foreground" Value="Black"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Opacity" Value="0.5"/>
                            <Setter Property="Foreground" Value="#B70000"/>
                        </Trigger>
                    </Style.Triggers>
                </Trigger>
            </Style.Triggers>
        </Style>-->

        <Style TargetType="TextBlock" x:Key="E_mail">
            <Setter Property="Opacity" Value="1"/>
            <Setter Property="Foreground" Value="#B70000"/>
            <Setter Property="Cursor" Value="Hand" />

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="TextDecorations" Value="Underline" />
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>

                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.13"/>
                    <Setter Property="Foreground" Value="Black"/>
                    <Setter Property="Cursor" Value="Arrow" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!--<Style TargetType="Label" x:Key="GWAN">
            <Setter Property="FontFamily" Value="./Fonts/InriaSerif/#Inria Serif"/>
        </Style>-->


    </Application.Resources>
</Application>
